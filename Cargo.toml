[package]
name = "qrich"
version = "0.2.0"
edition = "2021"
description = "Rich, visualized qstat and job-log navigator for the ABCI-Q PBS Professional cluster"
repository = "https://github.com/Wataru-Nakata/qrich"
license = "MIT"
readme = "README.md"

[dependencies]
serde_json = "1"

[profile.release]
opt-level = "s"
lto = true
strip = true
